import React, { Component } from 'react';

import { ScrollView } from 'react-native';

// import { Container } from './styles';

export default class LastBets extends Component {
  render() {
    return <ScrollView style={{ flex: 1, backgroundColor: 'blue' }}></ScrollView>;
  }
}
